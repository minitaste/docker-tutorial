[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mysite"
requires-python = ">=3.11"
version = "0.0.1"
dependencies = [
  "fastapi",
]
[tool.setuptools.packages.find]
where = ["src"]